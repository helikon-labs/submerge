## Submerge Base

Contains base-level definitions for for Submerge types, Substrate types, command-line arguments, and the `Supervisor` definition, which is used to spawn and supervise Submerge services.