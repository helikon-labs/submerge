//!
//! AUTO-GENERATED CODE - DO NOT EDIT!
//!
//! Submerge Crystal API v1
//! Source: api-spec/submerge-crystal-api.json
//! Version: 1.0.0
//! Generated by `oas3-gen v0.23.6`
//!
//! REST API for Submerge Crystal, the core indexer component of Submerge.
//! 
//! Submerge API endpoints are grouped under five resource sets:
//! - Blocks
//! - Calls
//! - Events
//! - Extrinsics
//! - Genesis
//! - Metadata
//! - Traces
//! - System
//! 
//! Public API is limited by 5 requests per second.
//! Request query parameters use `snake_case`; response fields use `camelCase`.

use serde::{Deserialize, Serialize};
use validator::Validate;
static REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^(?:\\d+|(0x)?[a-f0-9A-F]{64})$").expect("invalid regex")
});
static REGEX_CALLS_BY_EXTRINSIC_HASH_REQUEST_PATH_EXTRINSIC_HASH: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^(?:0x)?[0-9a-fA-F]{64}$").expect("invalid regex")
});
static REGEX_TRACES_REQUEST_QUERY_KEY_PREFIX: std::sync::LazyLock<regex::Regex> = std::sync::LazyLock::new(||
regex::Regex::new("^0x(?:[0-9a-f]{2})+$").expect("invalid regex"));
///32-byte Substrate account id as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
pub type AccountIdHex = String;
///20-byte address as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
pub type Address20Hex = String;
///32-byte address as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
pub type Address32Hex = String;
///A block in the blockchain.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Block {
    ///Discriminated union for the Substrate multi-address type.
    ///The `type` tag selects the variant; `value` carries the payload.
    pub author: Option<Box<MultiAddress>>,
    ///Number of events in the block.
    #[serde(rename = "eventCount")]
    pub event_count: i32,
    ///Number of extrinsics in the block.
    #[serde(rename = "extrinsicCount")]
    pub extrinsic_count: i32,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "extrinsicRoot")]
    pub extrinsic_root: String,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub hash: String,
    ///Block height.
    pub number: i64,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "parentHash")]
    pub parent_hash: String,
    ///Runtime spec version.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "stateRoot")]
    pub state_root: String,
    ///Block status.
    ///- `finalized`: Blocks that have been finalized on chain.
    ///- `proposed`: Blocks that have been authored but not yet finalized.
    ///- `pruned`: Blocks that have been removed or are no longer accessible.
    pub status: BlockStatus,
    ///Block timestamp. Milliseconds.
    pub timestamp: Option<i64>,
    ///Block weight in JSON format. Schema depends on runtime metadata.
    pub weight: Option<serde_json::Value>,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Block200Response {
    pub data: Vec<Block>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
pub type Block200Response2 = Vec<Block>;
///Block status.
///- `finalized`: Blocks that have been finalized on chain.
///- `proposed`: Blocks that have been authored but not yet finalized.
///- `pruned`: Blocks that have been removed or are no longer accessible.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum BlockStatus {
    #[serde(rename = "proposed")]
    #[default]
    Proposed,
    #[serde(rename = "pruned")]
    Pruned,
    #[serde(rename = "finalized")]
    Finalized,
}
impl core::fmt::Display for BlockStatus {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Proposed => write!(f, "proposed"),
            Self::Pruned => write!(f, "pruned"),
            Self::Finalized => write!(f, "finalized"),
        }
    }
}
///If a hash is passed, returns the matching block. If a number is passed, gives the blocks by that number - could be multiple blocks if there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct BlocksByReferenceRequest {
    #[validate(nested)]
    pub path: BlocksByReferenceRequestPath,
}
#[bon::bon]
impl BlocksByReferenceRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_ref: String) -> anyhow::Result<Self> {
        let request = Self {
            path: BlocksByReferenceRequestPath {
                block_ref,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl BlocksByReferenceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<BlocksByReferenceResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Block200Response2,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksByReferenceResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksByReferenceResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksByReferenceResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksByReferenceResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksByReferenceResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(BlocksByReferenceResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct BlocksByReferenceRequestPath {
    ///Block reference. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
}
///Response types for get_blocks_by_reference
#[derive(Debug, Clone)]
pub enum BlocksByReferenceResponse {
    ///200: List of matching blocks.
    Ok(Block200Response2),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns all blocks from the database that satisfy the query parameters. It will return a paginated response, ordered descending by block number.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct BlocksRequest {
    #[validate(nested)]
    pub query: BlocksRequestQuery,
}
#[bon::bon]
impl BlocksRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        page: Option<i32>,
        page_size: Option<i32>,
        status: Option<BlockStatus>,
        min_block_number: Option<i64>,
        max_block_number: Option<i64>,
        min_block_timestamp: Option<i64>,
        max_block_timestamp: Option<i64>,
        min_spec_version: Option<i32>,
        max_spec_version: Option<i32>,
        author: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: BlocksRequestQuery {
                page,
                page_size,
                status,
                min_block_number,
                max_block_number,
                min_block_timestamp,
                max_block_timestamp,
                min_spec_version,
                max_spec_version,
                author,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl BlocksRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<BlocksResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Block200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(BlocksResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(BlocksResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlocksRequestQuery {
    ///Block list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of blocks per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter results by block status. If not specified, all blocks are returned.
    pub status: Option<BlockStatus>,
    ///Filter blocks by minimum block number.
    ///- Example: `Some(1_534_287i64)`
    #[validate(range(min = 0i64))]
    pub min_block_number: Option<i64>,
    ///Filter blocks by maximum block number.
    ///- Example: `Some(2_825_701i64)`
    #[validate(range(min = 0i64))]
    pub max_block_number: Option<i64>,
    ///Filter blocks by minimum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub min_block_timestamp: Option<i64>,
    ///Filter blocks by maximum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub max_block_timestamp: Option<i64>,
    ///Filter blocks by minimum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub min_spec_version: Option<i32>,
    ///Filter blocks by maximum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub max_spec_version: Option<i32>,
    ///Filter results by block author. Either of the following:
    ///- Author's Substrate SS58 address string (e.g. `5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY`).
    ///- Author's Substrate account id encoded as a hexadecimal string (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    ///- Author's address encoded as a hexadecimal string with optional `0x` prefix (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    ///- Example: `Some("5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY".to_string())`
    pub author: Option<String>,
}
///Response types for get_blocks
#[derive(Debug, Clone)]
pub enum BlocksResponse {
    ///200: Paginated list of matching blocks.
    Ok(Block200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///A runtime call.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Call {
    ///Call arguments in JSON format. Schema depends on the call's schema definition in the runtime metadata.
    pub args: Option<serde_json::Value>,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "blockHash")]
    pub block_hash: String,
    ///Number of the call's block.
    #[serde(rename = "blockNumber")]
    pub block_number: i64,
    ///Block status.
    ///- `finalized`: Blocks that have been finalized on chain.
    ///- `proposed`: Blocks that have been authored but not yet finalized.
    ///- `pruned`: Blocks that have been removed or are no longer accessible.
    #[serde(rename = "blockStatus")]
    pub block_status: BlockStatus,
    ///Timestamp of the call's block. Milliseconds.
    #[serde(rename = "blockTimestamp")]
    pub block_timestamp: Option<i64>,
    ///Index of the call, represented as an unsigned integer array.
    #[serde(rename = "callIndex")]
    pub call_index: Vec<i32>,
    ///Rust-style path of the call represented with parameter names or indices.
    #[serde(rename = "callPath")]
    pub call_path: String,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "extrinsicHash")]
    pub extrinsic_hash: String,
    ///Index of the call's extrinsic.
    #[serde(rename = "extrinsicIndex")]
    pub extrinsic_index: i32,
    ///Whether the call's extrinsic was successful.
    ///Note: The extrinsic can be successful where the call has failed (see the `Utility.ForceBatch`` call).
    #[serde(rename = "extrinsicIsSuccessful")]
    pub extrinsic_is_successful: bool,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub hash: String,
    ///Index of the call in its pallet.
    #[serde(rename = "palletCallIndex")]
    pub pallet_call_index: i32,
    ///Name of the call.
    #[serde(rename = "palletCallName")]
    pub pallet_call_name: String,
    ///Pallet index of the call.
    #[serde(rename = "palletIndex")]
    pub pallet_index: i32,
    ///Pallet name of the call.
    #[serde(rename = "palletName")]
    pub pallet_name: String,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "parentCallHash")]
    pub parent_call_hash: Option<String>,
    ///Runtime spec version of the call's block.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Call200Response {
    pub data: Vec<Call>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
pub type Call200Response2 = Vec<MetadataCall>;
///Call arguments wrapper.
pub type CallArgs = serde_json::Value;
///Returns the arguments of a runtime call by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallArgsByHashRequest {
    #[validate(nested)]
    pub path: CallArgsByHashRequestPath,
}
#[bon::bon]
impl CallArgsByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(call_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: CallArgsByHashRequestPath {
                call_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallArgsByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<CallArgsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                CallArgs,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(CallArgsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallArgsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallArgsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallArgsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallArgsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(CallArgsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallArgsByHashRequestPath {
    ///Call hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub call_hash: String,
}
///Response types for get_call_args_by_hash
#[derive(Debug, Clone)]
pub enum CallArgsByHashResponse {
    ///200: Arguments for the runtime call with the given hash.
    Ok(CallArgs),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the call by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallByHashRequest {
    #[validate(nested)]
    pub path: CallByHashRequestPath,
    pub query: CallByHashRequestQuery,
}
#[bon::bon]
impl CallByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(call_hash: String, include_args: Option<bool>) -> anyhow::Result<Self> {
        let request = Self {
            path: CallByHashRequestPath { call_hash },
            query: CallByHashRequestQuery {
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<CallByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<Call>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(CallByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallByHashRequestPath {
    ///Call hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub call_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct CallByHashRequestQuery {
    ///Whether to include call arguments in the call(s) in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_call_by_hash
#[derive(Debug, Clone)]
pub enum CallByHashResponse {
    ///200: Call with the given hash.
    Ok(Call),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the extrinsic of a call by call hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallExtrinsicByHashRequest {
    #[validate(nested)]
    pub path: CallExtrinsicByHashRequestPath,
}
#[bon::bon]
impl CallExtrinsicByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(call_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: CallExtrinsicByHashRequestPath {
                call_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallExtrinsicByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ExtrinsicByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Extrinsic,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(ExtrinsicByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallExtrinsicByHashRequestPath {
    ///Hash of the extrinsic call in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub call_hash: String,
}
///Returns the calls for extrinsic in a block by block reference and 0-based extrinsic index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByBlockReferenceAndExtrinsicIndexRequest {
    #[validate(nested)]
    pub path: CallsByBlockReferenceAndExtrinsicIndexRequestPath,
    #[validate(nested)]
    pub query: CallsByBlockReferenceAndExtrinsicIndexRequestQuery,
}
#[bon::bon]
impl CallsByBlockReferenceAndExtrinsicIndexRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        extrinsic_index: i32,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_call_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: CallsByBlockReferenceAndExtrinsicIndexRequestPath {
                block_ref,
                extrinsic_index,
            },
            query: CallsByBlockReferenceAndExtrinsicIndexRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_call_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallsByBlockReferenceAndExtrinsicIndexRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubCallsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubCallsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByBlockReferenceAndExtrinsicIndexRequestPath {
    ///Block reference for the extrinsic. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
    ///0-based index for the extrinsic in the block.
    #[validate(range(min = 0i32))]
    pub extrinsic_index: i32,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct CallsByBlockReferenceAndExtrinsicIndexRequestQuery {
    ///Block call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter blocks calls by pallet name. Case insensitive.
    ///- Example: `Some("system".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks calls by call name. Case insensitive.
    ///- Example: `Some("setcode".to_string())`
    pub pallet_call_name: Option<String>,
    ///Whether to include call arguments in the block calls in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///If a hash is passed, returns the calls for the matching block. If a number is passed, gives the calls for the blocks with that number - could be multiple blocks if there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByBlockReferenceRequest {
    #[validate(nested)]
    pub path: CallsByBlockReferenceRequestPath,
    #[validate(nested)]
    pub query: CallsByBlockReferenceRequestQuery,
}
#[bon::bon]
impl CallsByBlockReferenceRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_call_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: CallsByBlockReferenceRequestPath {
                block_ref,
            },
            query: CallsByBlockReferenceRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_call_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallsByBlockReferenceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubCallsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubCallsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByBlockReferenceRequestPath {
    ///Block reference for the calls. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct CallsByBlockReferenceRequestQuery {
    ///Block call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter blocks calls by pallet name. Case insensitive.
    ///- Example: `Some("system".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks calls by call name. Case insensitive.
    ///- Example: `Some("setcode".to_string())`
    pub pallet_call_name: Option<String>,
    ///Whether to include call arguments in the block calls in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Returns the calls in an extrinsic by extrinsic hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByExtrinsicHashRequest {
    #[validate(nested)]
    pub path: CallsByExtrinsicHashRequestPath,
    #[validate(nested)]
    pub query: CallsByExtrinsicHashRequestQuery,
}
#[bon::bon]
impl CallsByExtrinsicHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        extrinsic_hash: String,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_call_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: CallsByExtrinsicHashRequestPath {
                extrinsic_hash,
            },
            query: CallsByExtrinsicHashRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_call_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallsByExtrinsicHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubCallsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubCallsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct CallsByExtrinsicHashRequestPath {
    ///Extrinsic hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_CALLS_BY_EXTRINSIC_HASH_REQUEST_PATH_EXTRINSIC_HASH")
    )]
    pub extrinsic_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct CallsByExtrinsicHashRequestQuery {
    ///Block call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter blocks calls by pallet name. Case insensitive.
    ///- Example: `Some("system".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks calls by call name. Case insensitive.
    ///- Example: `Some("setcode".to_string())`
    pub pallet_call_name: Option<String>,
    ///Whether to include call arguments in the block calls in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Returns all calls from the database that satisfy the query parameters. It will return a paginated response, ordered descending by block number, then call id.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct CallsRequest {
    #[validate(nested)]
    pub query: CallsRequestQuery,
}
#[bon::bon]
impl CallsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        page: Option<i32>,
        page_size: Option<i32>,
        min_block_number: Option<i64>,
        max_block_number: Option<i64>,
        min_block_timestamp: Option<i64>,
        max_block_timestamp: Option<i64>,
        min_spec_version: Option<i32>,
        max_spec_version: Option<i32>,
        pallet_name: Option<String>,
        call_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: CallsRequestQuery {
                page,
                page_size,
                min_block_number,
                max_block_number,
                min_block_timestamp,
                max_block_timestamp,
                min_spec_version,
                max_spec_version,
                pallet_name,
                call_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl CallsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<CallsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(CallsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallsResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(CallsResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct CallsRequestQuery {
    ///Call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter calls by minimum block number.
    ///- Example: `Some(1_534_287i64)`
    #[validate(range(min = 0i64))]
    pub min_block_number: Option<i64>,
    ///Filter calls by maximum block number.
    ///- Example: `Some(2_825_701i64)`
    #[validate(range(min = 0i64))]
    pub max_block_number: Option<i64>,
    ///Filter calls by minimum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub min_block_timestamp: Option<i64>,
    ///Filter calls by maximum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub max_block_timestamp: Option<i64>,
    ///Filter calls by minimum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub min_spec_version: Option<i32>,
    ///Filter calls by maximum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub max_spec_version: Option<i32>,
    ///Filter calls by pallet name. Case insensitive. Stored in `camelCase`.
    ///- Example: `Some("system".to_string())`
    pub pallet_name: Option<String>,
    ///Filter calls by call name. Case insensitive. Stored in `camelCase`.
    ///- Example: `Some("setcode".to_string())`
    pub call_name: Option<String>,
    ///Whether to include call arguments in the calls in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_calls
#[derive(Debug, Clone)]
pub enum CallsResponse {
    ///200: Paginated list of matching calls.
    Ok(Call200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
pub type Constant200Response = Vec<MetadataConstant>;
///Generic error type.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Error {
    ///Error message.
    pub message: String,
}
pub type Error200Response = Vec<MetadataError>;
///A runtime event.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Event {
    ///Call arguments in JSON format. Schema depends on the call's schema definition in the runtime metadata.
    pub args: Option<serde_json::Value>,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "blockHash")]
    pub block_hash: String,
    ///Number of the event's block.
    #[serde(rename = "blockNumber")]
    pub block_number: i64,
    ///Block status.
    ///- `finalized`: Blocks that have been finalized on chain.
    ///- `proposed`: Blocks that have been authored but not yet finalized.
    ///- `pruned`: Blocks that have been removed or are no longer accessible.
    #[serde(rename = "blockStatus")]
    pub block_status: BlockStatus,
    ///Timestamp of the event's block. Milliseconds.
    #[serde(rename = "blockTimestamp")]
    pub block_timestamp: Option<i64>,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "extrinsicHash")]
    pub extrinsic_hash: Option<String>,
    ///Index of the event's extrinsic, if it was output from an extrinsic.
    #[serde(rename = "extrinsicIndex")]
    pub extrinsic_index: Option<i32>,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub hash: String,
    ///Index of the event in block.
    pub index: i32,
    ///Index of the event in its pallet.
    #[serde(rename = "palletEventIndex")]
    pub pallet_event_index: i32,
    ///Name of the event.
    #[serde(rename = "palletEventName")]
    pub pallet_event_name: String,
    ///Pallet index of the event.
    #[serde(rename = "palletIndex")]
    pub pallet_index: i32,
    ///Pallet name of the event.
    #[serde(rename = "palletName")]
    pub pallet_name: String,
    ///Event phase.
    pub phase: String,
    ///Runtime spec version of the event's block.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
    ///Index of the event's trace, if traces are enabled.
    #[serde(rename = "traceIndex")]
    pub trace_index: Option<i32>,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Event200Response {
    pub data: Vec<Event>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
pub type Event200Response2 = Vec<Event>;
pub type Event200Response3 = Vec<MetadataEvent>;
///Event arguments wrapper.
pub type EventArgs = serde_json::Value;
///Returns the arguments of a runtime call by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventArgsByHashRequest {
    #[validate(nested)]
    pub path: EventArgsByHashRequestPath,
}
#[bon::bon]
impl EventArgsByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(event_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: EventArgsByHashRequestPath {
                event_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventArgsByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventArgsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                EventArgs,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventArgsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventArgsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventArgsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventArgsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventArgsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventArgsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventArgsByHashRequestPath {
    ///Event hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub event_hash: String,
}
///Response types for get_event_args_by_hash
#[derive(Debug, Clone)]
pub enum EventArgsByHashResponse {
    ///200: Arguments for the runtime event with the given hash.
    Ok(EventArgs),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the event by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventByHashRequest {
    #[validate(nested)]
    pub path: EventByHashRequestPath,
    pub query: EventByHashRequestQuery,
}
#[bon::bon]
impl EventByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(event_hash: String, include_args: Option<bool>) -> anyhow::Result<Self> {
        let request = Self {
            path: EventByHashRequestPath {
                event_hash,
            },
            query: EventByHashRequestQuery {
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<Event>::json_with_diagnostics(req)
                .await?;
            return Ok(EventByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventByHashRequestPath {
    ///Event hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub event_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct EventByHashRequestQuery {
    ///Whether to include event arguments in the event(s) in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_event_by_hash
#[derive(Debug, Clone)]
pub enum EventByHashResponse {
    ///200: Event with the given hash.
    Ok(Event),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the events for extrinsic in a block by block reference and 0-based extrinsic index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceAndExtrinsicIndexRequest {
    #[validate(nested)]
    pub path: EventsByBlockReferenceAndExtrinsicIndexRequestPath,
    #[validate(nested)]
    pub query: EventsByBlockReferenceAndExtrinsicIndexRequestQuery,
}
#[bon::bon]
impl EventsByBlockReferenceAndExtrinsicIndexRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        extrinsic_index: i32,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_event_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: EventsByBlockReferenceAndExtrinsicIndexRequestPath {
                block_ref,
                extrinsic_index,
            },
            query: EventsByBlockReferenceAndExtrinsicIndexRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_event_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventsByBlockReferenceAndExtrinsicIndexRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventsByExtrinsicHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventsByExtrinsicHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceAndExtrinsicIndexRequestPath {
    ///Block reference for the extrinsic. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
    ///0-based index for the extrinsic in the block.
    #[validate(range(min = 0i32))]
    pub extrinsic_index: i32,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct EventsByBlockReferenceAndExtrinsicIndexRequestQuery {
    ///Block event list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block events per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter events calls by pallet name. Case insensitive.
    ///- Example: `Some("staking".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks events by event name. Case insensitive.
    ///- Example: `Some("bonded".to_string())`
    pub pallet_event_name: Option<String>,
    ///Whether to include event arguments in the block events in the response.
    ///Default is `false`. Setting this to `true` increases response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Returns the events in the given block at the given index. It can return multiple events if a number is passed and there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceAndIndexRequest {
    #[validate(nested)]
    pub path: EventsByBlockReferenceAndIndexRequestPath,
    pub query: EventsByBlockReferenceAndIndexRequestQuery,
}
#[bon::bon]
impl EventsByBlockReferenceAndIndexRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        event_index: i32,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: EventsByBlockReferenceAndIndexRequestPath {
                block_ref,
                event_index,
            },
            query: EventsByBlockReferenceAndIndexRequestQuery {
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventsByBlockReferenceAndIndexRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventsByBlockReferenceAndIndexResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response2,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByBlockReferenceAndIndexResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByBlockReferenceAndIndexResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByBlockReferenceAndIndexResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByBlockReferenceAndIndexResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByBlockReferenceAndIndexResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventsByBlockReferenceAndIndexResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceAndIndexRequestPath {
    ///Block reference for the event(s). Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
    ///0-based index for the event(s) in the block(s).
    #[validate(range(min = 0i32))]
    pub event_index: i32,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceAndIndexRequestQuery {
    ///Whether to include event arguments in the event(s) in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_events_by_block_reference_and_index
#[derive(Debug, Clone)]
pub enum EventsByBlockReferenceAndIndexResponse {
    ///200: List of matching events.
    Ok(Event200Response2),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///If a hash is passed, returns the events for the matching block. If a number is passed, gives the events for the block with that number - could be multiple blocks if there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceRequest {
    #[validate(nested)]
    pub path: EventsByBlockReferenceRequestPath,
    #[validate(nested)]
    pub query: EventsByBlockReferenceRequestQuery,
}
#[bon::bon]
impl EventsByBlockReferenceRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_event_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: EventsByBlockReferenceRequestPath {
                block_ref,
            },
            query: EventsByBlockReferenceRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_event_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventsByBlockReferenceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventsByExtrinsicHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventsByExtrinsicHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByBlockReferenceRequestPath {
    ///Block reference for the events. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct EventsByBlockReferenceRequestQuery {
    ///Block event list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block events per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter events calls by pallet name. Case insensitive.
    ///- Example: `Some("staking".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks events by event name. Case insensitive.
    ///- Example: `Some("bonded".to_string())`
    pub pallet_event_name: Option<String>,
    ///Whether to include event arguments in the block events in the response.
    ///Default is `false`. Setting this to `true` increases response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Returns the events in an extrinsic by extrinsic hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByExtrinsicHashRequest {
    #[validate(nested)]
    pub path: EventsByExtrinsicHashRequestPath,
    #[validate(nested)]
    pub query: EventsByExtrinsicHashRequestQuery,
}
#[bon::bon]
impl EventsByExtrinsicHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        extrinsic_hash: String,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_event_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: EventsByExtrinsicHashRequestPath {
                extrinsic_hash,
            },
            query: EventsByExtrinsicHashRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_event_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventsByExtrinsicHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventsByExtrinsicHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsByExtrinsicHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventsByExtrinsicHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct EventsByExtrinsicHashRequestPath {
    ///Extrinsic hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_CALLS_BY_EXTRINSIC_HASH_REQUEST_PATH_EXTRINSIC_HASH")
    )]
    pub extrinsic_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct EventsByExtrinsicHashRequestQuery {
    ///Block event list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block events per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter events calls by pallet name. Case insensitive.
    ///- Example: `Some("staking".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks events by event name. Case insensitive.
    ///- Example: `Some("bonded".to_string())`
    pub pallet_event_name: Option<String>,
    ///Whether to include event arguments in the block events in the response.
    ///Default is `false`. Setting this to `true` increases response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_events_by_extrinsic_hash
#[derive(Debug, Clone)]
pub enum EventsByExtrinsicHashResponse {
    ///200: Paginated list of matching events.
    Ok(Event200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns all events from the database that satisfy the query parameters. It will return a paginated response, ordered descending by block number and ascending event index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventsRequest {
    #[validate(nested)]
    pub query: EventsRequestQuery,
}
#[bon::bon]
impl EventsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        page: Option<i32>,
        page_size: Option<i32>,
        min_block_number: Option<i64>,
        max_block_number: Option<i64>,
        min_block_timestamp: Option<i64>,
        max_block_timestamp: Option<i64>,
        min_spec_version: Option<i32>,
        max_spec_version: Option<i32>,
        pallet_name: Option<String>,
        event_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: EventsRequestQuery {
                page,
                page_size,
                min_block_number,
                max_block_number,
                min_block_timestamp,
                max_block_timestamp,
                min_spec_version,
                max_spec_version,
                pallet_name,
                event_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(EventsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventsResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct EventsRequestQuery {
    ///Events list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of events per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter events by minimum block number.
    ///- Example: `Some(1_534_287i64)`
    #[validate(range(min = 0i64))]
    pub min_block_number: Option<i64>,
    ///Filter events by maximum block number.
    ///- Example: `Some(2_825_701i64)`
    #[validate(range(min = 0i64))]
    pub max_block_number: Option<i64>,
    ///Filter events by minimum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub min_block_timestamp: Option<i64>,
    ///Filter events by maximum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub max_block_timestamp: Option<i64>,
    ///Filter events by minimum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub min_spec_version: Option<i32>,
    ///Filter events by maximum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub max_spec_version: Option<i32>,
    ///Filter events by pallet name. Case insensitive. Stored in `camelCase`.
    ///- Example: `Some("balances".to_string())`
    pub pallet_name: Option<String>,
    ///Filter events by event name. Case insensitive. Stored in `camelCase`.
    ///- Example: `Some("transfer".to_string())`
    pub event_name: Option<String>,
    ///Whether to include event arguments in the events in the response.
    ///Default is `false`. Setting this to `true` increases response size considerably.
    ///Prefer to use the `GET /event/{event_hash}/args` endpoint per event instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_events
#[derive(Debug, Clone)]
pub enum EventsResponse {
    ///200: Paginated list of matching events.
    Ok(Event200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///An extrinsic in a block. Signed or unsigned.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Extrinsic {
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "blockHash")]
    pub block_hash: String,
    ///Number of the extrinsic's block.
    #[serde(rename = "blockNumber")]
    pub block_number: i64,
    ///Block status.
    ///- `finalized`: Blocks that have been finalized on chain.
    ///- `proposed`: Blocks that have been authored but not yet finalized.
    ///- `pruned`: Blocks that have been removed or are no longer accessible.
    #[serde(rename = "blockStatus")]
    pub block_status: BlockStatus,
    ///Timestamp of the extrinsic's block. Milliseconds.
    #[serde(rename = "blockTimestamp")]
    pub block_timestamp: Option<i64>,
    ///Extrinsic extras in JSON format - checkNonce, checkGenesis, chargeTransactionPayment, etc.
    pub extra: Option<serde_json::Value>,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub hash: String,
    ///Extrinsic index.
    pub index: i32,
    ///Whether the extrinsic was successful.
    #[serde(rename = "isSuccessful")]
    pub is_successful: bool,
    ///Discriminated union for the Substrate multi-signature type.
    ///The `type` tag selects the variant; `value` carries the payload.
    pub signature: Option<Box<MultiSignature>>,
    ///Discriminated union for the Substrate multi-address type.
    ///The `type` tag selects the variant; `value` carries the payload.
    pub signer: Option<Box<MultiAddress>>,
    ///Runtime spec version of the extrinsic's block.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
    ///Trace index for the extrinsic.
    #[serde(rename = "traceIndex")]
    pub trace_index: Option<i32>,
    ///Extrinsic version in metadata.
    pub version: i32,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Extrinsic200Response {
    pub data: Vec<Extrinsic>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
pub type Extrinsic200Response2 = Vec<Extrinsic>;
///Returns the extrinsic by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicByHashRequest {
    #[validate(nested)]
    pub path: ExtrinsicByHashRequestPath,
}
#[bon::bon]
impl ExtrinsicByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(extrinsic_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: ExtrinsicByHashRequestPath {
                extrinsic_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ExtrinsicByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ExtrinsicByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Extrinsic,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(ExtrinsicByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicByHashRequestPath {
    ///Extrinsic hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub extrinsic_hash: String,
}
///Response types for get_extrinsic_by_hash
#[derive(Debug, Clone)]
pub enum ExtrinsicByHashResponse {
    ///200: Extrinsic with the given hash.
    Ok(Extrinsic),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the root call of an extrinsic by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicRootCallByHashRequest {
    #[validate(nested)]
    pub path: ExtrinsicRootCallByHashRequestPath,
    pub query: ExtrinsicRootCallByHashRequestQuery,
}
#[bon::bon]
impl ExtrinsicRootCallByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        extrinsic_hash: String,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: ExtrinsicRootCallByHashRequestPath {
                extrinsic_hash,
            },
            query: ExtrinsicRootCallByHashRequestQuery {
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ExtrinsicRootCallByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<CallByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<Call>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(CallByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicRootCallByHashRequestPath {
    ///Extrinsic hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub extrinsic_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct ExtrinsicRootCallByHashRequestQuery {
    ///Whether to include call arguments in the call(s) in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Returns the extrinsics in the given block at the given index. It can return multiple extrinsics if a number is passed and there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicsByBlockReferenceAndIndexRequest {
    #[validate(nested)]
    pub path: ExtrinsicsByBlockReferenceAndIndexRequestPath,
}
#[bon::bon]
impl ExtrinsicsByBlockReferenceAndIndexRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_ref: String, extrinsic_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: ExtrinsicsByBlockReferenceAndIndexRequestPath {
                block_ref,
                extrinsic_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ExtrinsicsByBlockReferenceAndIndexRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ExtrinsicsByBlockReferenceAndIndexResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Extrinsic200Response2,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceAndIndexResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceAndIndexResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceAndIndexResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceAndIndexResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(
                ExtrinsicsByBlockReferenceAndIndexResponse::InternalServerError(data),
            );
        }
        let _ = req.bytes().await?;
        return Ok(ExtrinsicsByBlockReferenceAndIndexResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicsByBlockReferenceAndIndexRequestPath {
    ///Block reference for the extrinsic. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
    ///0-based index for the extrinsic in the block.
    #[validate(range(min = 0i32))]
    pub extrinsic_index: i32,
}
///Response types for get_extrinsics_by_block_reference_and_index
#[derive(Debug, Clone)]
pub enum ExtrinsicsByBlockReferenceAndIndexResponse {
    ///200: List of matching extrinsics.
    Ok(Extrinsic200Response2),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///If a hash is passed, returns the extrinsics for the matching block. If a number is passed, gives the extrinsis for the block by that number - could be multiple blocks if there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicsByBlockReferenceRequest {
    #[validate(nested)]
    pub path: ExtrinsicsByBlockReferenceRequestPath,
    #[validate(nested)]
    pub query: ExtrinsicsByBlockReferenceRequestQuery,
}
#[bon::bon]
impl ExtrinsicsByBlockReferenceRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        page: Option<i32>,
        page_size: Option<i32>,
        is_signed: Option<bool>,
        signer: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: ExtrinsicsByBlockReferenceRequestPath {
                block_ref,
            },
            query: ExtrinsicsByBlockReferenceRequestQuery {
                page,
                page_size,
                is_signed,
                signer,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ExtrinsicsByBlockReferenceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ExtrinsicsByBlockReferenceResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Extrinsic200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsByBlockReferenceResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(ExtrinsicsByBlockReferenceResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicsByBlockReferenceRequestPath {
    ///Block reference for the extrinsics. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    ///- Example: `"13453264".to_string()`
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ExtrinsicsByBlockReferenceRequestQuery {
    ///Block call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Whether to include only signed/unsigned extrinsics within the block.
    ///- Example: `Some(true)`
    pub is_signed: Option<bool>,
    ///Filter block extrinsics by signer. Either of the following:
    ///- Signer's Substrate SS58 address string (e.g. `5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY`).
    ///- Signer's Substrate account id encoded as a hexadecimal string (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    ///- Signer's address encoded as a hexadecimal string with optional `0x` prefix (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    pub signer: Option<String>,
}
///Response types for get_extrinsics_by_block_reference
#[derive(Debug, Clone)]
pub enum ExtrinsicsByBlockReferenceResponse {
    ///200: Paginated list of matching extrinsics.
    Ok(Extrinsic200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns all extrinsics from the database that satisfy the query parameters. It will return a paginated response, ordered descending by block number and ascending extrinsic index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ExtrinsicsRequest {
    #[validate(nested)]
    pub query: ExtrinsicsRequestQuery,
}
#[bon::bon]
impl ExtrinsicsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        page: Option<i32>,
        page_size: Option<i32>,
        min_block_number: Option<i64>,
        max_block_number: Option<i64>,
        min_block_timestamp: Option<i64>,
        max_block_timestamp: Option<i64>,
        min_spec_version: Option<i32>,
        max_spec_version: Option<i32>,
        is_signed: Option<bool>,
        signer: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: ExtrinsicsRequestQuery {
                page,
                page_size,
                min_block_number,
                max_block_number,
                min_block_timestamp,
                max_block_timestamp,
                min_spec_version,
                max_spec_version,
                is_signed,
                signer,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ExtrinsicsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ExtrinsicsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Extrinsic200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(ExtrinsicsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(ExtrinsicsResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ExtrinsicsRequestQuery {
    ///Extrinsic list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of extrinsics per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter extrinsics by minimum block number.
    ///- Example: `Some(1_534_287i64)`
    #[validate(range(min = 0i64))]
    pub min_block_number: Option<i64>,
    ///Filter extrinsics by maximum block number.
    ///- Example: `Some(2_825_701i64)`
    #[validate(range(min = 0i64))]
    pub max_block_number: Option<i64>,
    ///Filter extrinsics by minimum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub min_block_timestamp: Option<i64>,
    ///Filter extrinsics by maximum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub max_block_timestamp: Option<i64>,
    ///Filter extrinsics by minimum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub min_spec_version: Option<i32>,
    ///Filter extrinsics by maximum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub max_spec_version: Option<i32>,
    ///Whether to include only signed/unsigned extrinsics.
    ///- Example: `Some(true)`
    pub is_signed: Option<bool>,
    ///Filter extrinsics by signer. Either of the following:
    ///- Signer's Substrate SS58 address string (e.g. `5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY`).
    ///- Signer's Substrate account id encoded as a hexadecimal string (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    ///- Signer's address encoded as a hexadecimal string with optional `0x` prefix (e.g. `0x008d8404893c7b4b80f397605cc96e61fec3c89676c8c2794a2a7d281d678b1a`).
    ///- Example: `Some("5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY".to_string())`
    pub signer: Option<String>,
}
///Response types for get_extrinsics
#[derive(Debug, Clone)]
pub enum ExtrinsicsResponse {
    ///200: Paginated list of matching extrinsics.
    Ok(Extrinsic200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Genesi200Response {
    pub data: Vec<GenesisRecord>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
///A storage item initialized at genesis.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct GenesisRecord {
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "keyParams")]
    pub key_params: Option<String>,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "keyPrefix")]
    pub key_prefix: String,
    ///If the record is a known UTF-8 key, the string representation of the key.
    #[serde(rename = "knownKey")]
    pub known_key: Option<String>,
    ///If the record is a storage item, the storage item's pallet index in the metadata.
    #[serde(rename = "palletIndex")]
    pub pallet_index: Option<i32>,
    ///If the record is a storage item, the storage item's pallet name in the metadata.
    #[serde(rename = "palletName")]
    pub pallet_name: Option<String>,
    ///If the record is a storage item, the storage item's index in the pallet metadata.
    #[serde(rename = "palletStorageItemIndex")]
    pub pallet_storage_item_index: Option<i32>,
    ///If the record is a storage item, the storage item's name.
    #[serde(rename = "palletStorageItemName")]
    pub pallet_storage_item_name: Option<String>,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    pub value: String,
}
///Returns a paginated list of all genesis storage records.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GenesisRecordsRequest {
    #[validate(nested)]
    pub query: GenesisRecordsRequestQuery,
}
#[bon::bon]
impl GenesisRecordsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(page: Option<i32>, page_size: Option<i32>) -> anyhow::Result<Self> {
        let request = Self {
            query: GenesisRecordsRequestQuery {
                page,
                page_size,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GenesisRecordsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GenesisRecordsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Genesi200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GenesisRecordsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(GenesisRecordsResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(GenesisRecordsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(GenesisRecordsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GenesisRecordsResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GenesisRecordsRequestQuery {
    ///Genesis record list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of genesis records per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
}
///Response types for get_genesis_records
#[derive(Debug, Clone)]
pub enum GenesisRecordsResponse {
    ///200: Paginated list of genesis record items.
    Ok(Genesi200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
pub type Hash256Hex = String;
///Arbitrary hex-encoded bytes, `0x`-prefixed.
pub type HexString = String;
///Arbitrary hex-encoded bytes, mixed case, optional `0x`-prefix.
pub type HexStringParam = String;
///Parsed metadata specification complete with its pallets.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Metadata {
    ///Metadata version.
    #[serde(rename = "metadataVersion")]
    pub metadata_version: i32,
    ///Metadata pallets.
    pub pallets: Vec<MetadataPallet>,
    ///Metadata runtime spec version.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
}
///A call defined in a metadata pallet.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataCall {
    ///Metadata item documentation. One item per line in the array.
    pub docs: Vec<String>,
    ///Call's index inside the metadata pallet.
    pub index: i32,
    ///Call name. Camel case.
    pub name: String,
}
///A constant defined in a metadata pallet.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataConstant {
    ///Metadata item documentation. One item per line in the array.
    pub docs: Vec<String>,
    ///Constant's index inside the metadata pallet.
    pub index: i32,
    ///Constant name. Camel case.
    pub name: String,
    ///Id of the constant's type.
    #[serde(rename = "typeId")]
    pub type_id: Option<i32>,
    ///Name of the constant's type.
    #[serde(rename = "typeName")]
    pub type_name: String,
    ///Metadata constant value in JSON format. Schema depends on the definition within the runtime metadata.
    pub value: Option<serde_json::Value>,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "valueHex")]
    pub value_hex: String,
}
///An error defined in a metadata pallet.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataError {
    ///Metadata item documentation. One item per line in the array.
    pub docs: Vec<String>,
    ///Error's index inside the metadata pallet.
    pub index: i32,
    ///Error name. Camel case.
    pub name: String,
}
///An event defined in a metadata pallet.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataEvent {
    ///Metadata item documentation. One item per line in the array.
    pub docs: Vec<String>,
    ///Event's index inside the metadata pallet.
    pub index: i32,
    ///Event name. Camel case.
    pub name: String,
}
///Returns the SCALE-encoded hexadecimal string for the prefixed metadata.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataHexRequest {
    #[validate(nested)]
    pub path: MetadataHexRequestPath,
}
#[bon::bon]
impl MetadataHexRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataHexRequestPath {
                spec_version,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataHexRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataHexResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                HexString,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataHexResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataHexResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataHexResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataHexResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataHexResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataHexResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataHexRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
}
///Response types for get_metadata_hex
#[derive(Debug, Clone)]
pub enum MetadataHexResponse {
    ///200: SCALE-encoded prefixed metadata hexadecimal string.
    Ok(HexString),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Metadata item documentation. One item per line in the array.
pub type MetadataItemDocumentation = Vec<String>;
///Original Substrate runtime metadata in JSON representation.
pub type MetadataJSON = serde_json::Value;
///Returns the original Substrate runtime metadata by its spec version in JSON representation.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataJsonRequest {
    #[validate(nested)]
    pub path: MetadataJsonRequestPath,
}
#[bon::bon]
impl MetadataJsonRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataJsonRequestPath {
                spec_version,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataJsonRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataJsonResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                MetadataJSON,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataJsonResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataJsonResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataJsonResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataJsonResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataJsonResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataJsonResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataJsonRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
}
///Response types for get_metadata_json
#[derive(Debug, Clone)]
pub enum MetadataJsonResponse {
    ///200: Full metadata with the given spec version.
    Ok(MetadataJSON),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns a list of metadata summaries for all runtime versions.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataListRequest {
    #[validate(nested)]
    pub query: MetadataListRequestQuery,
}
#[bon::bon]
impl MetadataListRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(page: Option<i32>, page_size: Option<i32>) -> anyhow::Result<Self> {
        let request = Self {
            query: MetadataListRequestQuery {
                page,
                page_size,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataListRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataListResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Metadatum200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataListResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataListResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataListResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataListResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataListResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct MetadataListRequestQuery {
    ///Page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 0i32))]
    pub page: Option<i32>,
    ///Number of items per page to be returned.
    ///- Example: `Some(100i32)`
    #[validate(range(min = 0i32))]
    pub page_size: Option<i32>,
}
///Response types for get_metadata_list
#[derive(Debug, Clone)]
pub enum MetadataListResponse {
    ///200: Response with a paginated list of metadata summaries.
    Ok(Metadatum200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///A pallet defined in metadata, with its calls, constants, errors, events, and storage items.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataPallet {
    ///Pallet's calls.
    pub calls: Vec<MetadataCall>,
    ///Pallet's constants.
    pub constants: Vec<MetadataConstant>,
    ///Pallet's errors.
    pub errors: Vec<MetadataError>,
    ///Pallet's events.
    pub events: Vec<MetadataEvent>,
    ///Pallet's index inside the metadata.
    pub index: i32,
    ///Pallet's name.
    pub name: String,
    ///Pallet's storage items.
    #[serde(rename = "storageItems")]
    pub storage_items: Vec<MetadataStorageItem>,
}
///Returns the calls in a pallet in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletCallsRequest {
    #[validate(nested)]
    pub path: MetadataPalletCallsRequestPath,
}
#[bon::bon]
impl MetadataPalletCallsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletCallsRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletCallsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletCallsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response2,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletCallsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletCallsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletCallsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletCallsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletCallsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletCallsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletCallsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet_calls
#[derive(Debug, Clone)]
pub enum MetadataPalletCallsResponse {
    ///200: All calls in a pallet.
    Ok(Call200Response2),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the constants in a pallet in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletConstantsRequest {
    #[validate(nested)]
    pub path: MetadataPalletConstantsRequestPath,
}
#[bon::bon]
impl MetadataPalletConstantsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletConstantsRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletConstantsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletConstantsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Constant200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletConstantsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletConstantsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletConstantsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletConstantsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletConstantsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletConstantsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletConstantsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet_constants
#[derive(Debug, Clone)]
pub enum MetadataPalletConstantsResponse {
    ///200: All constants in a pallet.
    Ok(Constant200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the errors in a pallet in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletErrorsRequest {
    #[validate(nested)]
    pub path: MetadataPalletErrorsRequestPath,
}
#[bon::bon]
impl MetadataPalletErrorsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletErrorsRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletErrorsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletErrorsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Error200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletErrorsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletErrorsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletErrorsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletErrorsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletErrorsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletErrorsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletErrorsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet_errors
#[derive(Debug, Clone)]
pub enum MetadataPalletErrorsResponse {
    ///200: All errors in a pallet.
    Ok(Error200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the events in a pallet in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletEventsRequest {
    #[validate(nested)]
    pub path: MetadataPalletEventsRequestPath,
}
#[bon::bon]
impl MetadataPalletEventsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletEventsRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletEventsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletEventsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Event200Response3,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletEventsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletEventsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletEventsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletEventsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletEventsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletEventsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletEventsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet_events
#[derive(Debug, Clone)]
pub enum MetadataPalletEventsResponse {
    ///200: All events in a pallet.
    Ok(Event200Response3),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the full metadata pallet by metadata spec version and pallet index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletRequest {
    #[validate(nested)]
    pub path: MetadataPalletRequestPath,
}
#[bon::bon]
impl MetadataPalletRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                MetadataPallet,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletRequestPath {
    ///Metadata spec version.
    ///- Example: `2_000_000i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet
#[derive(Debug, Clone)]
pub enum MetadataPalletResponse {
    ///200: Full metadata pallet with the given spec version and pallet index.
    Ok(MetadataPallet),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the storage items in a pallet in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletStorageItemsRequest {
    #[validate(nested)]
    pub path: MetadataPalletStorageItemsRequestPath,
}
#[bon::bon]
impl MetadataPalletStorageItemsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32, pallet_index: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletStorageItemsRequestPath {
                spec_version,
                pallet_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletStorageItemsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletStorageItemsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Storage200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletStorageItemsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletStorageItemsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletStorageItemsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletStorageItemsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletStorageItemsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletStorageItemsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletStorageItemsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
    ///Metadata pallet index.
    ///- Example: `50i32`
    #[validate(range(min = 0i32))]
    pub pallet_index: i32,
}
///Response types for get_metadata_pallet_storage_items
#[derive(Debug, Clone)]
pub enum MetadataPalletStorageItemsResponse {
    ///200: All storage items in a pallet.
    Ok(Storage200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///The summary of a pallet defined in metadata, with only its index and name.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataPalletSummary {
    ///Pallet's index inside the metadata.
    pub index: i32,
    ///Pallet's name.
    pub name: String,
}
///Returns pallets in a metadata version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletsRequest {
    #[validate(nested)]
    pub path: MetadataPalletsRequestPath,
}
#[bon::bon]
impl MetadataPalletsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataPalletsRequestPath {
                spec_version,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataPalletsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataPalletsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Pallet200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletsResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletsResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataPalletsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataPalletsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataPalletsRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
}
///Response types for get_metadata_pallets
#[derive(Debug, Clone)]
pub enum MetadataPalletsResponse {
    ///200: List of summaries of matching metadata pallets.
    Ok(Pallet200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns the metadata by its spec version.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataRequest {
    #[validate(nested)]
    pub path: MetadataRequestPath,
}
#[bon::bon]
impl MetadataRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(spec_version: i32) -> anyhow::Result<Self> {
        let request = Self {
            path: MetadataRequestPath {
                spec_version,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl MetadataRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<MetadataResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Metadata,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(MetadataResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(MetadataResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct MetadataRequestPath {
    ///Metadata spec version.
    ///- Example: `1_003i32`
    #[validate(range(min = 0i32))]
    pub spec_version: i32,
}
///Response types for get_metadata
#[derive(Debug, Clone)]
pub enum MetadataResponse {
    ///200: Full metadata with the given spec version.
    Ok(Metadata),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///A storage item defined in a metadata pallet.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataStorageItem {
    ///Metadata item documentation. One item per line in the array.
    pub docs: Vec<String>,
    ///Storage item's index inside the metadata pallet.
    pub index: i32,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "keyPrefix")]
    pub key_prefix: String,
    ///Storage item name. Camel case.
    pub name: String,
}
///Basic fields to represent a runtime version metadata.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct MetadataSummary {
    ///Metadata version.
    #[serde(rename = "metadataVersion")]
    pub metadata_version: i32,
    ///Metadata runtime spec version.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Metadatum200Response {
    pub data: Vec<MetadataSummary>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
///Discriminated union for the Substrate multi-address type.
///The `type` tag selects the variant; `value` carries the payload.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum MultiAddress {
    ///32-byte account id.
    #[default]
    AddressAccountId(MultiAddressAccountId),
    ///Account index.
    AddressAccountIndex(MultiAddressAccountIndex),
    ///Raw account address.
    AddressRaw(MultiAddressRaw),
    ///20-byte address.
    Address20(MultiAddress20),
    ///32-byte address.
    Address32(MultiAddress32),
}
impl MultiAddress {
    ///32-byte account id.
    pub fn account_id(value: String) -> Self {
        Self::AddressAccountId(MultiAddressAccountId {
            value,
            ..Default::default()
        })
    }
    ///Account index.
    pub fn account_index(value: i32) -> Self {
        Self::AddressAccountIndex(MultiAddressAccountIndex {
            value,
            ..Default::default()
        })
    }
    ///Raw account address.
    pub fn raw(value: String) -> Self {
        Self::AddressRaw(MultiAddressRaw {
            value,
            ..Default::default()
        })
    }
    ///20-byte address.
    pub fn address20(value: String) -> Self {
        Self::Address20(MultiAddress20 {
            value,
            ..Default::default()
        })
    }
    ///32-byte address.
    pub fn address32(value: String) -> Self {
        Self::Address32(MultiAddress32 {
            value,
            ..Default::default()
        })
    }
}
///20-byte address.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiAddress20 {
    ///Multi-address 20-byte address type.
    #[serde(rename = "type")]
    #[default("address20".to_string())]
    pub r#type: String,
    ///20-byte address as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
    pub value: String,
}
///Multi-address 20-byte address type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiAddress20Type {
    #[serde(rename = "address20")]
    #[default]
    Address20,
}
impl core::fmt::Display for MultiAddress20Type {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Address20 => write!(f, "address20"),
        }
    }
}
///32-byte address.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiAddress32 {
    ///Multi-address 32-byte address type.
    #[serde(rename = "type")]
    #[default("address32".to_string())]
    pub r#type: String,
    ///32-byte address as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
    pub value: String,
}
///Multi-address 32-byte address type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiAddress32Type {
    #[serde(rename = "address32")]
    #[default]
    Address32,
}
impl core::fmt::Display for MultiAddress32Type {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Address32 => write!(f, "address32"),
        }
    }
}
///32-byte account id.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiAddressAccountId {
    ///Multi-address 32-byte account id type.
    #[serde(rename = "type")]
    #[default("accountId".to_string())]
    pub r#type: String,
    ///32-byte Substrate account id as lowercase hex, **always** `0x`-prefixed and lowercase in responses.
    pub value: String,
}
///Multi-address 32-byte account id type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiAddressAccountIdType {
    #[serde(rename = "accountId")]
    #[default]
    AccountId,
}
impl core::fmt::Display for MultiAddressAccountIdType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::AccountId => write!(f, "accountId"),
        }
    }
}
///Account index.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiAddressAccountIndex {
    ///Multi-address account index type.
    #[serde(rename = "type")]
    #[default("index".to_string())]
    pub r#type: String,
    ///Account index value.
    pub value: i32,
}
///Multi-address account index type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiAddressIndexType {
    #[serde(rename = "index")]
    #[default]
    Index,
}
impl core::fmt::Display for MultiAddressIndexType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Index => write!(f, "index"),
        }
    }
}
///Raw account address.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiAddressRaw {
    ///Multi-address raw account address type.
    #[serde(rename = "type")]
    #[default("raw".to_string())]
    pub r#type: String,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    pub value: String,
}
///Multi-address raw account address type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiAddressRawType {
    #[serde(rename = "raw")]
    #[default]
    Raw,
}
impl core::fmt::Display for MultiAddressRawType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Raw => write!(f, "raw"),
        }
    }
}
///Discriminated union for the Substrate multi-signature type.
///The `type` tag selects the variant; `value` carries the payload.
#[derive(Debug, Clone, PartialEq)]
pub enum MultiSignature {
    ECDSA(MultiSignatureECDSA),
    Ed25519(MultiSignatureEd25519),
    Eth(MultiSignatureEth),
    Sr25519(MultiSignatureSr25519),
}
impl MultiSignature {
    pub const DISCRIMINATOR_FIELD: &'static str = "type";
}
impl Default for MultiSignature {
    fn default() -> Self {
        Self::ECDSA(<MultiSignatureECDSA>::default())
    }
}
impl<'de> serde::Deserialize<'de> for MultiSignature {
    fn deserialize<D>(deserializer: D) -> core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let value = serde_json::Value::deserialize(deserializer)?;
        match value.get(Self::DISCRIMINATOR_FIELD).and_then(|v| v.as_str()) {
            Some("ecdsa") => {
                serde_json::from_value(value)
                    .map(Self::ECDSA)
                    .map_err(serde::de::Error::custom)
            }
            Some("ed25519") => {
                serde_json::from_value(value)
                    .map(Self::Ed25519)
                    .map_err(serde::de::Error::custom)
            }
            Some("eth") => {
                serde_json::from_value(value)
                    .map(Self::Eth)
                    .map_err(serde::de::Error::custom)
            }
            Some("sr25519") => {
                serde_json::from_value(value)
                    .map(Self::Sr25519)
                    .map_err(serde::de::Error::custom)
            }
            None => Err(serde::de::Error::missing_field(Self::DISCRIMINATOR_FIELD)),
            Some(other) => {
                Err(
                    serde::de::Error::custom(
                        format!(
                            "Unknown discriminator value '{}' for field '{}'", other,
                            "type"
                        ),
                    ),
                )
            }
        }
    }
}
impl MultiSignature {
    ///ECDSA signature.
    pub fn ecdsa(value: String) -> Self {
        Self::ECDSA(MultiSignatureECDSA {
            value,
            ..Default::default()
        })
    }
    ///Eth signature.
    pub fn eth(value: String) -> Self {
        Self::Eth(MultiSignatureEth {
            value,
            ..Default::default()
        })
    }
    ///Ed25519 signature.
    pub fn ed25519(value: String) -> Self {
        Self::Ed25519(MultiSignatureEd25519 {
            value,
            ..Default::default()
        })
    }
    ///Sr25519 signature.
    pub fn sr25519(value: String) -> Self {
        Self::Sr25519(MultiSignatureSr25519 {
            value,
            ..Default::default()
        })
    }
}
///ECDSA signature.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiSignatureECDSA {
    ///Multi-signature ECDSA type.
    #[serde(rename = "type")]
    #[default("ecdsa".to_string())]
    pub r#type: String,
    ///and ECDSA (65 bytes → 130 hex chars). **Always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub value: String,
}
///Multi-signature ECDSA type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiSignatureECDSAType {
    #[serde(rename = "ecdsa")]
    #[default]
    Ecdsa,
}
impl core::fmt::Display for MultiSignatureECDSAType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Ecdsa => write!(f, "ecdsa"),
        }
    }
}
///Ed25519 signature.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiSignatureEd25519 {
    ///Multi-signature Ed25519 type.
    #[serde(rename = "type")]
    #[default("ed25519".to_string())]
    pub r#type: String,
    ///and ECDSA (65 bytes → 130 hex chars). **Always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub value: String,
}
///Multi-signature Ed25519 type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiSignatureEd25519Type {
    #[serde(rename = "ed25519")]
    #[default]
    Ed25519,
}
impl core::fmt::Display for MultiSignatureEd25519Type {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Ed25519 => write!(f, "ed25519"),
        }
    }
}
///Eth signature.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiSignatureEth {
    ///Multi-signature Eth type (ECDSA over secp256k1, applied to a Keccak-256 hash).
    #[serde(rename = "type")]
    #[default("eth".to_string())]
    pub r#type: String,
    ///and ECDSA (65 bytes → 130 hex chars). **Always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub value: String,
}
///Multi-signature Eth type (ECDSA over secp256k1, applied to a Keccak-256 hash).
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiSignatureEthType {
    #[serde(rename = "eth")]
    #[default]
    Eth,
}
impl core::fmt::Display for MultiSignatureEthType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Eth => write!(f, "eth"),
        }
    }
}
///Sr25519 signature.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct MultiSignatureSr25519 {
    ///Multi-signature Sr25519 type.
    #[serde(rename = "type")]
    #[default("sr25519".to_string())]
    pub r#type: String,
    ///and ECDSA (65 bytes → 130 hex chars). **Always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub value: String,
}
///Multi-signature Sr25519 type.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum MultiSignatureSr25519Type {
    #[serde(rename = "sr25519")]
    #[default]
    Sr25519,
}
impl core::fmt::Display for MultiSignatureSr25519Type {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Sr25519 => write!(f, "sr25519"),
        }
    }
}
///Pagination data for paged responses.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct PaginationData {
    ///Current page number. 1-indexed.
    pub page: i32,
    ///Number of items per page.
    #[serde(rename = "pageSize")]
    pub page_size: i32,
    ///Total number of items across all pages.
    pub total: i64,
}
pub type Pallet200Response = Vec<MetadataPalletSummary>;
///Returns a parent call by the sub call's hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ParentCallByHashRequest {
    #[validate(nested)]
    pub path: ParentCallByHashRequestPath,
    pub query: ParentCallByHashRequestQuery,
}
#[bon::bon]
impl ParentCallByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(call_hash: String, include_args: Option<bool>) -> anyhow::Result<Self> {
        let request = Self {
            path: ParentCallByHashRequestPath {
                call_hash,
            },
            query: ParentCallByHashRequestQuery {
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ParentCallByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<CallByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<Call>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(CallByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(CallByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ParentCallByHashRequestPath {
    ///Sub call hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub call_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct ParentCallByHashRequestQuery {
    ///Whether to include call arguments in the call(s) in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///and ECDSA (65 bytes → 130 hex chars). **Always** `0x`-prefixed and lowercase in responses.
///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
pub type SignatureHexString = String;
pub type Storage200Response = Vec<MetadataStorageItem>;
///Returns sub calls call by a parent call's hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubCallsByHashRequest {
    #[validate(nested)]
    pub path: SubCallsByHashRequestPath,
    #[validate(nested)]
    pub query: SubCallsByHashRequestQuery,
}
#[bon::bon]
impl SubCallsByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        call_hash: String,
        page: Option<i32>,
        page_size: Option<i32>,
        pallet_name: Option<String>,
        pallet_call_name: Option<String>,
        include_args: Option<bool>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: SubCallsByHashRequestPath {
                call_hash,
            },
            query: SubCallsByHashRequestQuery {
                page,
                page_size,
                pallet_name,
                pallet_call_name,
                include_args,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl SubCallsByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubCallsByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Call200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(SubCallsByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubCallsByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct SubCallsByHashRequestPath {
    ///Parent call hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub call_hash: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SubCallsByHashRequestQuery {
    ///Block call list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of block calls per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter blocks calls by pallet name. Case insensitive.
    ///- Example: `Some("system".to_string())`
    pub pallet_name: Option<String>,
    ///Filter blocks calls by call name. Case insensitive.
    ///- Example: `Some("setcode".to_string())`
    pub pallet_call_name: Option<String>,
    ///Whether to include call arguments in the block calls in the response.
    ///Default is `false`. Setting this to `true` increases the response size considerably.
    ///Prefer to use the `GET /call/{call_hash}/args` endpoint per call instead.
    #[default(Some(false))]
    pub include_args: Option<bool>,
}
///Response types for get_sub_calls_by_hash
#[derive(Debug, Clone)]
pub enum SubCallsByHashResponse {
    ///200: Paginated list of matching calls.
    Ok(Call200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///A trace record in a block.
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Trace {
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    #[serde(rename = "blockHash")]
    pub block_hash: String,
    ///Number of the trace's block.
    #[serde(rename = "blockNumber")]
    pub block_number: i64,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "extId")]
    pub ext_id: String,
    ///32-byte Blake2b-256, **always** `0x`-prefixed and lowercase in responses.
    ///Inputs elsewhere may accept mixed case or missing `0x`; the API normalizes outputs.
    pub hash: String,
    ///Index of the trace within the block.
    pub index: i32,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "keyParams")]
    pub key_params: Option<String>,
    ///Arbitrary hex-encoded bytes, `0x`-prefixed.
    #[serde(rename = "keyPrefix")]
    pub key_prefix: String,
    ///If the trace record is for a known UTF-8 key, the string representation of the key.
    #[serde(rename = "knownKey")]
    pub known_key: Option<String>,
    ///If the trace record is a storage item, the storage item's pallet index in the metadata.
    #[serde(rename = "palletIndex")]
    pub pallet_index: Option<i32>,
    ///If the trace record is a storage item, the storage item's pallet name in the metadata.
    #[serde(rename = "palletName")]
    pub pallet_name: Option<String>,
    ///If the trace record is a storage item, the storage item's index in the pallet metadata.
    #[serde(rename = "palletStorageItemIndex")]
    pub pallet_storage_item_index: Option<i32>,
    ///If the trace record is a storage item, the storage item's name.
    #[serde(rename = "palletStorageItemName")]
    pub pallet_storage_item_name: Option<String>,
    #[serde(rename = "parentId")]
    pub parent_id: Option<String>,
    ///Runtime spec version of the call's block.
    #[serde(rename = "specVersion")]
    pub spec_version: i32,
    ///Block trace storage method.
    #[serde(rename = "storageMethod")]
    pub storage_method: TraceStorageMethod,
}
#[derive(Debug, Clone, PartialEq, Deserialize, oas3_gen_support::Default)]
pub struct Trace200Response {
    pub data: Vec<Trace>,
    ///Pagination data for paged responses.
    pub pagination: PaginationData,
}
///Returns the trace by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct TraceByHashRequest {
    #[validate(nested)]
    pub path: TraceByHashRequestPath,
}
#[bon::bon]
impl TraceByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(trace_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: TraceByHashRequestPath {
                trace_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl TraceByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<TraceByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<Trace>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceByHashResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(TraceByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct TraceByHashRequestPath {
    ///Trace hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub trace_hash: String,
}
///Response types for get_trace_by_hash
#[derive(Debug, Clone)]
pub enum TraceByHashResponse {
    ///200: Trace with the given hash.
    Ok(Trace),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Block trace storage method.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Deserialize, oas3_gen_support::Default)]
pub enum TraceStorageMethod {
    #[serde(rename = "Put")]
    #[default]
    Put,
    #[serde(rename = "ChildPut")]
    ChildPut,
    #[serde(rename = "ChildKill")]
    ChildKill,
    #[serde(rename = "ClearPrefix")]
    ClearPrefix,
    #[serde(rename = "ChildClearPrefix")]
    ChildClearPrefix,
    #[serde(rename = "Append")]
    Append,
    #[serde(rename = "Genesis")]
    Genesis,
}
impl core::fmt::Display for TraceStorageMethod {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Put => write!(f, "Put"),
            Self::ChildPut => write!(f, "ChildPut"),
            Self::ChildKill => write!(f, "ChildKill"),
            Self::ClearPrefix => write!(f, "ClearPrefix"),
            Self::ChildClearPrefix => write!(f, "ChildClearPrefix"),
            Self::Append => write!(f, "Append"),
            Self::Genesis => write!(f, "Genesis"),
        }
    }
}
///Returns the value of a trace record by its hash.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct TraceValueByHashRequest {
    #[validate(nested)]
    pub path: TraceValueByHashRequestPath,
}
#[bon::bon]
impl TraceValueByHashRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(trace_hash: String) -> anyhow::Result<Self> {
        let request = Self {
            path: TraceValueByHashRequestPath {
                trace_hash,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl TraceValueByHashRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<TraceValueByHashResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                HexString,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(TraceValueByHashResponse::Ok(data));
        }
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(TraceValueByHashResponse::NoContent);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceValueByHashResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceValueByHashResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceValueByHashResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TraceValueByHashResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(TraceValueByHashResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct TraceValueByHashRequestPath {
    ///Trace hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub trace_hash: String,
}
///Response types for get_trace_value_by_hash
#[derive(Debug, Clone)]
pub enum TraceValueByHashResponse {
    ///200: SCALE-encoded value of the storage trace.
    Ok(HexString),
    ///204: Trace exists but has no value (null).
    NoContent,
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///If a hash is passed, returns the traces for the matching block. If a number is passed, returns the traces for the blocks with that number - could be multiple blocks if there's a pruned block in that slot.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct TracesByBlockReferenceRequest {
    #[validate(nested)]
    pub path: TracesByBlockReferenceRequestPath,
    #[validate(nested)]
    pub query: TracesByBlockReferenceRequestQuery,
}
#[bon::bon]
impl TracesByBlockReferenceRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_ref: String,
        page: Option<i32>,
        page_size: Option<i32>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: TracesByBlockReferenceRequestPath {
                block_ref,
            },
            query: TracesByBlockReferenceRequestQuery {
                page,
                page_size,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl TracesByBlockReferenceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<TracesByBlockReferenceResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Trace200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(TracesByBlockReferenceResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesByBlockReferenceResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesByBlockReferenceResponse::NotFound(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesByBlockReferenceResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesByBlockReferenceResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(TracesByBlockReferenceResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct TracesByBlockReferenceRequestPath {
    ///Block reference for the traces. Either a block number (integer ≥ 0), or a block hash in hex (with or without `0x` prefix, case-insensitive).
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_BLOCKS_BY_REFERENCE_REQUEST_PATH_BLOCK_REF")
    )]
    pub block_ref: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct TracesByBlockReferenceRequestQuery {
    ///Block traces list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of traces traces in block per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
}
///Response types for get_traces_by_block_reference
#[derive(Debug, Clone)]
pub enum TracesByBlockReferenceResponse {
    ///200: Paginated list of matching traces.
    Ok(Trace200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///404: Item not found.
    NotFound(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}
///Returns all traces from the database that satisfy the query parameters. It will return a paginated response, ordered descending by block number, then ascending trace index.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct TracesRequest {
    #[validate(nested)]
    pub query: TracesRequestQuery,
}
#[bon::bon]
impl TracesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        page: Option<i32>,
        page_size: Option<i32>,
        min_block_number: Option<i64>,
        max_block_number: Option<i64>,
        min_block_timestamp: Option<i64>,
        max_block_timestamp: Option<i64>,
        min_spec_version: Option<i32>,
        max_spec_version: Option<i32>,
        key_prefix: Option<String>,
        key_params: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: TracesRequestQuery {
                page,
                page_size,
                min_block_number,
                max_block_number,
                min_block_timestamp,
                max_block_timestamp,
                min_spec_version,
                max_spec_version,
                key_prefix,
                key_params,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl TracesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<TracesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                Trace200Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(TracesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesResponse::BadRequest(data));
        }
        if status.as_u16() == 429u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesResponse::TooManyRequests(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<Error>::json_with_diagnostics(req)
                .await?;
            return Ok(TracesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(TracesResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct TracesRequestQuery {
    ///Trace list page number to retrieve. 1-indexed.
    ///- Example: `Some(1i32)`
    #[validate(range(min = 1i32))]
    #[default(Some(1i32))]
    pub page: Option<i32>,
    ///Number of traces per page to be returned.
    ///- Example: `Some(50i32)`
    #[validate(range(min = 1i32, max = 100i32))]
    #[default(Some(25i32))]
    pub page_size: Option<i32>,
    ///Filter traces by minimum block number.
    ///- Example: `Some(1_534_287i64)`
    #[validate(range(min = 0i64))]
    pub min_block_number: Option<i64>,
    ///Filter traces by maximum block number.
    ///- Example: `Some(2_825_701i64)`
    #[validate(range(min = 0i64))]
    pub max_block_number: Option<i64>,
    ///Filter traces by minimum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub min_block_timestamp: Option<i64>,
    ///Filter traces by maximum block timestamp. In milliseconds.
    ///- Example: `Some(1_755_773_684_012i64)`
    #[validate(range(min = 0i64))]
    pub max_block_timestamp: Option<i64>,
    ///Filter traces by minimum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub min_spec_version: Option<i32>,
    ///Filter traces by maximum runtime spec version.
    ///- Example: `Some(1_090i32)`
    #[validate(range(min = 0i32))]
    pub max_spec_version: Option<i32>,
    ///Filter traces by Substrate storage key prefix.
    #[validate(regex(path = "REGEX_TRACES_REQUEST_QUERY_KEY_PREFIX"))]
    pub key_prefix: Option<String>,
    ///Filter traces by Substrate storage key parameters.
    #[validate(regex(path = "REGEX_TRACES_REQUEST_QUERY_KEY_PREFIX"))]
    pub key_params: Option<String>,
}
///Response types for get_traces
#[derive(Debug, Clone)]
pub enum TracesResponse {
    ///200: Paginated list of matching traces.
    Ok(Trace200Response),
    ///400: Invalid path or query parameter(s).
    BadRequest(Error),
    ///429: Rate limit exceeded.
    TooManyRequests(Error),
    ///500: Internal server error.
    InternalServerError(Error),
    ///default: Unknown response
    Unknown,
}

